import { logging } from "protractor";

export class Cliente {
    dni!: number;
    name!: string;
    surname!: string;
    email!: string;
    password!: string
    telephone!: number;
    enabled!: boolean;
}
